name: rfid_mobil
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
    
  firebase_auth:

  cupertino_icons: ^1.0.8

  # 🔥 Firebase
  firebase_core: ^3.15.2
  cloud_firestore: ^5.6.12
  flutter_map: ^8.2.2
  http: ^1.6.0
  latlong2: ^0.9.1
  provider: ^6.1.5+1

  # 🔥 NFC
  nfc_manager: ^4.2.1
  image_picker: ^1.0.7
  pytorch_lite: ^4.3.2
  google_sign_in: ^6.3.0
  googleapis:
  extension_google_sign_in_as_googleapis_auth:

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/models/hackathon_model.ptl
    - assets/models/labels.txt
